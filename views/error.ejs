<%- include('partials/header') %>

<div class="text-center py-20">
    <div class="text-6xl mb-4">⚠️</div>
    <h1 class="text-3xl font-bold text-slate-800 mb-3"><%= title %></h1>
    <p class="text-slate-500 mb-8"><%= message %></p>
    <a href="<%= backUrl %>" class="inline-block bg-indigo-600 text-white font-semibold px-6 py-2 rounded-lg hover:bg-indigo-700 transition">← Go Back</a>
</div>

<%- include('partials/footer') %>
